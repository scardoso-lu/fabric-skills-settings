# Fabric Agent Pack — placeholder-only target repository environment template.
# Copy to .env locally if needed. Never commit .env or real secret values.

# Fabric Platform
FABRIC_WORKSPACE_PATH=            # e.g. MySandbox.Workspace
FABRIC_WORKSPACE_ID=              # placeholder only; fill locally in .env
FABRIC_CAPACITY_NAME=             # e.g. F2, P1
PIPELINE_ENV=sandbox              # sandbox | dev | prod

# Lakehouse IDs — placeholders only
BRONZE_LAKEHOUSE_ID=
SILVER_LAKEHOUSE_ID=
GOLD_LAKEHOUSE_ID=

# Sandbox data source examples
# SRC_ORDERS_TYPE=file
# SRC_ORDERS_PATH=./data/sandbox/orders.csv
